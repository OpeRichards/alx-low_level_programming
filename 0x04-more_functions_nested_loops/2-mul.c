#include "main.h"

/**
 * mul - Multiply two numbers and prints result
 * @a: One of the numbers to multiply
 * @b: The second number to multiply
 * Return: Return result when performed
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
